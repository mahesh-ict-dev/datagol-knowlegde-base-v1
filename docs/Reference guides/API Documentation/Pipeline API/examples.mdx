# Examples

This section provides practical examples of how to use the Pipeline API.

## Creating a Pipeline

### Basic Pipeline Creation

```bash
curl -X POST "https://api.example.com/dataPipeline/api/v2/" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Daily Sales Report",
    "type": "STANDARD",
    "description": "Generates daily sales reports",
    "scheduleType": "CRON",
    "schedule": {
      "cronExpression": "0 2 * * *",
      "timeZone": "UTC"
    },
    "sources": [
      {
        "id": 123,
        "name": "Sales Database",
        "type": "POSTGRESQL"
      }
    ],
    "destination": {
      "id": 456,
      "name": "Data Warehouse",
      "type": "SNOWFLAKE"
    },
    "streams": [
      {
        "name": "sales_summary",
        "query": "SELECT date, SUM(amount) FROM sales GROUP BY date",
        "outputTableName": "daily_sales_summary",
        "syncMode": "FULL"
      }
    ],
    "queryEngine": "SPARK"
  }'
```

### Pipeline with Pre/Post Processing

```bash
curl -X POST "https://api.example.com/dataPipeline/api/v2/" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Customer Data Pipeline",
    "type": "CUSTOM",
    "description": "Processes customer data with validation and enrichment",
    "scheduleType": "SCHEDULED",
    "schedule": {
      "cronExpression": "0 */6 * * *",
      "timeZone": "UTC"
    },
    "sources": [
      {
        "id": 789,
        "name": "Customer Database",
        "type": "MYSQL"
      }
    ],
    "destination": {
      "id": 101,
      "name": "Analytics Warehouse",
      "type": "BIGQUERY"
    },
    "streams": [
      {
        "name": "customer_data",
        "query": "SELECT * FROM customers WHERE updated_at > :last_sync",
        "outputTableName": "processed_customers",
        "syncMode": "INCREMENTAL",
        "columns": ["id", "name", "email", "created_at", "updated_at"],
        "uniqueColumns": ["id"],
        "requiredParams": ["last_sync"]
      }
    ],
    "processes": [
      {
        "name": "validate_customer_data",
        "query": "UPDATE processed_customers SET status = 'VALIDATED' WHERE email IS NOT NULL",
        "processType": "PRE",
        "rank": 1
      },
      {
        "name": "enrich_customer_data",
        "query": "UPDATE processed_customers SET region = get_region_from_ip(ip_address)",
        "processType": "POST",
        "rank": 1
      }
    ],
    "parameters": {
      "last_sync": "2024-01-01T00:00:00Z"
    },
    "queryEngine": "SNOWFLAKE"
  }'
```

## Running a Pipeline

### Manual Pipeline Execution

```bash
curl -X POST "https://api.example.com/dataPipeline/api/v1/123/run" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "date": "2024-01-15",
    "region": "US"
  }'
```

### Running with Parameters

```bash
curl -X POST "https://api.example.com/dataPipeline/api/v1/456/run" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "last_sync": "2024-01-14T00:00:00Z",
    "batch_size": 1000,
    "dry_run": false
  }'
```

## Monitoring Pipeline Jobs

### Get Running Jobs

```bash
curl -X GET "https://api.example.com/dataPipeline/api/v1/getAllRunningJobs" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### Get Failed Jobs

```bash
curl -X GET "https://api.example.com/dataPipeline/api/v1/getAllFailedJobs/24/HOUR" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### Get Job Details

```bash
curl -X GET "https://api.example.com/dataPipeline/api/v1/123/job/789" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### Cancel a Running Job

```bash
curl -X GET "https://api.example.com/dataPipeline/api/v1/123/job/789/cancelSync" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

## JavaScript Examples

### Creating a Pipeline with JavaScript

```javascript
async function createPipeline(pipelineData) {
  const response = await fetch('/dataPipeline/api/v2/', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(pipelineData)
  });

  if (!response.ok) {
    throw new Error(`Failed to create pipeline: ${response.statusText}`);
  }

  return await response.json();
}

// Usage
const pipelineData = {
  name: 'E-commerce Analytics',
  type: 'STANDARD',
  description: 'Processes e-commerce transaction data',
  scheduleType: 'CRON',
  schedule: {
    cronExpression: '0 */4 * * *', // Every 4 hours
    timeZone: 'UTC'
  },
  sources: [
    {
      id: 234,
      name: 'E-commerce Database',
      type: 'POSTGRESQL'
    }
  ],
  destination: {
    id: 567,
    name: 'Analytics Platform',
    type: 'SNOWFLAKE'
  },
  streams: [
    {
      name: 'transactions',
      query: 'SELECT * FROM transactions WHERE created_at >= :start_date',
      outputTableName: 'processed_transactions',
      syncMode: 'INCREMENTAL',
      columns: ['id', 'user_id', 'amount', 'product_id', 'created_at'],
      uniqueColumns: ['id'],
      requiredParams: ['start_date']
    }
  ],
  parameters: {
    start_date: '2024-01-01T00:00:00Z'
  },
  queryEngine: 'SPARK'
};

createPipeline(pipelineData)
  .then(pipeline => console.log('Pipeline created:', pipeline))
  .catch(error => console.error('Error:', error));
```

### Running a Pipeline with JavaScript

```javascript
async function runPipeline(pipelineId, parameters = {}) {
  const response = await fetch(`/dataPipeline/api/v1/${pipelineId}/run`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(parameters)
  });

  if (!response.ok) {
    throw new Error(`Failed to run pipeline: ${response.statusText}`);
  }

  return await response.json();
}

// Usage
runPipeline(123, {
  date: new Date().toISOString().split('T')[0],
  region: 'US'
})
  .then(job => console.log('Job started:', job))
  .catch(error => console.error('Error:', error));
```

### Monitoring Pipeline Jobs with JavaScript

```javascript
async function getPipelineJobs(status = null, timeRange = null) {
  const requestBody = {};
  
  if (status) {
    requestBody.status = status;
  }
  
  if (timeRange) {
    requestBody.time = timeRange;
  }

  const response = await fetch('/dataPipeline/api/v2/job', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(requestBody)
  });

  if (!response.ok) {
    throw new Error(`Failed to get jobs: ${response.statusText}`);
  }

  return await response.json();
}

// Usage
getPipelineJobs('RUNNING')
  .then(result => console.log('Running jobs:', result.jobs))
  .catch(error => console.error('Error:', error));

getPipelineJobs('FAILED', { unit: 24, unitType: 'HOUR' })
  .then(result => console.log('Failed jobs in last 24 hours:', result.jobs))
  .catch(error => console.error('Error:', error));
```

## Python Examples

### Creating a Pipeline with Python

```python
import requests
import json
from datetime import datetime

def create_pipeline(pipeline_data, token):
    headers = {
        'Authorization': f'Bearer {token}',
        'Content-Type': 'application/json'
    }
    
    response = requests.post(
        '/dataPipeline/api/v2/',
        json=pipeline_data,
        headers=headers
    )
    
    response.raise_for_status()
    return response.json()

# Usage
pipeline_data = {
    'name': 'Inventory Pipeline',
    'type': 'STANDARD',
    'description': 'Syncs inventory data from multiple sources',
    'scheduleType': 'CRON',
    'schedule': {
        'cronExpression': '0 */2 * * *',  # Every 2 hours
        'timeZone': 'UTC'
    },
    'sources': [
        {
            'id': 345,
            'name': 'Inventory System',
            'type': 'REST_API'
        }
    ],
    'destination': {
        'id': 678,
        'name': 'Data Lake',
        'type': 'S3'
    },
    'streams': [
        {
            'name': 'inventory_levels',
            'query': 'SELECT product_id, quantity, warehouse_id FROM inventory',
            'outputTableName': 'current_inventory',
            'syncMode': 'FULL'
        }
    ],
    'queryEngine': 'SPARK'
}

try:
    pipeline = create_pipeline(pipeline_data, 'your-jwt-token')
    print(f"Pipeline created: {pipeline['id']}")
except requests.exceptions.RequestException as e:
    print(f"Error creating pipeline: {e}")
```

### Running a Pipeline with Python

```python
import requests
import time

def run_pipeline(pipeline_id, parameters=None, token=None):
    headers = {
        'Authorization': f'Bearer {token}',
        'Content-Type': 'application/json'
    }
    
    data = parameters or {}
    
    response = requests.post(
        f'/dataPipeline/api/v1/{pipeline_id}/run',
        json=data,
        headers=headers
    )
    
    response.raise_for_status()
    return response.json()

def wait_for_job_completion(pipeline_id, job_id, token, timeout=3600):
    """Wait for job completion with timeout"""
    start_time = time.time()
    
    while time.time() - start_time < timeout:
        response = requests.get(
            f'/dataPipeline/api/v1/{pipeline_id}/job/{job_id}',
            headers={'Authorization': f'Bearer {token}'}
        )
        
        if response.ok:
            job = response.json()
            status = job['status']
            
            if status in ['COMPLETED', 'SUCCEEDED']:
                return job
            elif status in ['FAILED', 'CANCELLED']:
                raise Exception(f"Job failed with status: {status}")
        
        time.sleep(10)  # Wait 10 seconds before checking again
    
    raise Exception("Job timeout")

# Usage
try:
    # Run pipeline
    job = run_pipeline(123, {
        'date': datetime.now().strftime('%Y-%m-%d'),
        'region': 'US'
    }, 'your-jwt-token')
    
    print(f"Job started: {job['id']}")
    
    # Wait for completion
    completed_job = wait_for_job_completion(123, job['id'], 'your-jwt-token')
    print(f"Job completed: {completed_job['status']}")
    
except Exception as e:
    print(f"Error: {e}")
```

### Monitoring Pipeline Jobs with Python

```python
import requests
from datetime import datetime, timedelta

def get_pipeline_jobs(status=None, time_range=None, token=None):
    headers = {
        'Authorization': f'Bearer {token}',
        'Content-Type': 'application/json'
    }
    
    request_body = {}
    
    if status:
        request_body['status'] = status
    
    if time_range:
        request_body['time'] = time_range
    
    response = requests.post(
        '/dataPipeline/api/v2/job',
        json=request_body,
        headers=headers
    )
    
    response.raise_for_status()
    return response.json()

def get_failed_jobs(hours=24, token=None):
    """Get failed jobs from the last N hours"""
    response = requests.get(
        f'/dataPipeline/api/v1/getAllFailedJobs/{hours}/HOUR',
        headers={'Authorization': f'Bearer {token}'}
    )
    
    response.raise_for_status()
    return response.json()

def get_job_error_log(pipeline_id, job_id, token=None):
    """Get error logs for a specific job"""
    response = requests.get(
        f'/dataPipeline/api/v1/{pipeline_id}/job/{job_id}/errorLog',
        headers={'Authorization': f'Bearer {token}'}
    )
    
    response.raise_for_status()
    return response.json()

# Usage
token = 'your-jwt-token'

# Get running jobs
running_jobs = get_pipeline_jobs(status='RUNNING', token=token)
print(f"Running jobs: {len(running_jobs['jobs'])}")

# Get failed jobs from last 24 hours
failed_jobs = get_failed_jobs(hours=24, token=token)
print(f"Failed jobs in last 24 hours: {len(failed_jobs['jobs'])}")

# Get error logs for a specific job
if failed_jobs['jobs']:
    first_failed_job = failed_jobs['jobs'][0]
    pipeline_id = first_failed_job['pipelineInfo']['pipelineId']
    job_id = first_failed_job['job']['id']
    
    error_logs = get_job_error_log(pipeline_id, job_id, token)
    print(f"Error logs for job {job_id}:")
    for log in error_logs['errorLogs']:
        print(f"  {log['timestamp']}: {log['message']}")
```

## React Hook Example

```typescript
import { useState, useEffect } from 'react';

interface Pipeline {
  id: number;
  name: string;
  type: string;
  description: string;
  scheduleType: string;
  status: string;
}

interface UsePipelinesReturn {
  pipelines: Pipeline[];
  loading: boolean;
  error: string | null;
  createPipeline: (pipelineData: Partial<Pipeline>) => Promise<void>;
  updatePipeline: (id: number, pipelineData: Partial<Pipeline>) => Promise<void>;
  deletePipeline: (id: number) => Promise<void>;
  runPipeline: (id: number, parameters?: any) => Promise<void>;
}

export function usePipelines(token: string): UsePipelinesReturn {
  const [pipelines, setPipelines] = useState<Pipeline[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const fetchPipelines = async () => {
    try {
      const response = await fetch('/dataPipeline/api/v2/', {
        headers: {
          'Authorization': `Bearer ${token}`
        }
      });

      if (!response.ok) {
        throw new Error('Failed to fetch pipelines');
      }

      const data = await response.json();
      setPipelines(data);
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Unknown error');
    } finally {
      setLoading(false);
    }
  };

  const createPipeline = async (pipelineData: Partial<Pipeline>) => {
    try {
      const response = await fetch('/dataPipeline/api/v2/', {
        method: 'POST',
        headers: {
          'Authorization': `Bearer ${token}`,
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(pipelineData)
      });

      if (!response.ok) {
        throw new Error('Failed to create pipeline');
      }

      await fetchPipelines(); // Refresh the list
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Unknown error');
    }
  };

  const updatePipeline = async (id: number, pipelineData: Partial<Pipeline>) => {
    try {
      const response = await fetch(`/dataPipeline/api/v2/${id}`, {
        method: 'PUT',
        headers: {
          'Authorization': `Bearer ${token}`,
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(pipelineData)
      });

      if (!response.ok) {
        throw new Error('Failed to update pipeline');
      }

      await fetchPipelines(); // Refresh the list
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Unknown error');
    }
  };

  const deletePipeline = async (id: number) => {
    try {
      const response = await fetch(`/dataPipeline/api/v1/${id}`, {
        method: 'DELETE',
        headers: {
          'Authorization': `Bearer ${token}`
        }
      });

      if (!response.ok) {
        throw new Error('Failed to delete pipeline');
      }

      await fetchPipelines(); // Refresh the list
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Unknown error');
    }
  };

  const runPipeline = async (id: number, parameters: any = {}) => {
    try {
      const response = await fetch(`/dataPipeline/api/v1/${id}/run`, {
        method: 'POST',
        headers: {
          'Authorization': `Bearer ${token}`,
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(parameters)
      });

      if (!response.ok) {
        throw new Error('Failed to run pipeline');
      }

      const job = await response.json();
      console.log('Pipeline job started:', job);
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Unknown error');
    }
  };

  useEffect(() => {
    fetchPipelines();
  }, [token]);

  return {
    pipelines,
    loading,
    error,
    createPipeline,
    updatePipeline,
    deletePipeline,
    runPipeline
  };
}

// Usage in React component
function PipelineList() {
  const { pipelines, loading, error, createPipeline, runPipeline } = usePipelines('your-jwt-token');

  if (loading) return <div>Loading pipelines...</div>;
  if (error) return <div>Error: {error}</div>;

  return (
    <div>
      <h2>Pipelines</h2>
      {pipelines.map(pipeline => (
        <div key={pipeline.id}>
          <h3>{pipeline.name}</h3>
          <p>{pipeline.description}</p>
          <button onClick={() => runPipeline(pipeline.id)}>
            Run Pipeline
          </button>
        </div>
      ))}
    </div>
  );
}
```

## Advanced Examples

### Pipeline with Complex Scheduling

```javascript
// Pipeline that runs on business days only
const businessDayPipeline = {
  name: 'Business Day Analytics',
  type: 'CUSTOM',
  description: 'Runs analytics only on business days',
  scheduleType: 'CRON',
  schedule: {
    cronExpression: '0 9 * * MON-FRI', // 9 AM on weekdays
    timeZone: 'America/New_York'
  },
  sources: [
    {
      id: 456,
      name: 'Business Data',
      type: 'ORACLE'
    }
  ],
  destination: {
    id: 789,
    name: 'Analytics Platform',
    type: 'SNOWFLAKE'
  },
  streams: [
    {
      name: 'business_metrics',
      query: `
        SELECT 
          date,
          SUM(revenue) as daily_revenue,
          COUNT(transactions) as transaction_count,
          AVG(amount) as avg_transaction_value
        FROM business_transactions 
        WHERE date = CURRENT_DATE
        GROUP BY date
      `,
      outputTableName: 'daily_business_metrics',
      syncMode: 'FULL'
    }
  ],
  queryEngine: 'SPARK'
};
```

### Pipeline with Data Validation

```python
# Pipeline with comprehensive data validation
validation_pipeline = {
    'name': 'Data Quality Pipeline',
    'type': 'CUSTOM',
    'description': 'Validates and cleans data before processing',
    'scheduleType': 'CRON',
    'schedule': {
        'cronExpression': '0 */4 * * *',  # Every 4 hours
        'timeZone': 'UTC'
    },
    'sources': [
        {
            'id': 123,
            'name': 'Raw Data Source',
            'type': 'CSV'
        }
    ],
    'destination': {
        'id': 456,
        'name': 'Cleaned Data Warehouse',
        'type': 'BIGQUERY'
    },
    'streams': [
        {
            'name': 'raw_data_ingestion',
            'query': 'SELECT * FROM raw_data WHERE created_at >= :last_sync',
            'outputTableName': 'staging_data',
            'syncMode': 'INCREMENTAL',
            'requiredParams': ['last_sync']
        }
    ],
    'processes': [
        {
            'name': 'validate_email_format',
            'query': '''
                UPDATE staging_data 
                SET email_valid = CASE 
                    WHEN email REGEXP '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$' 
                    THEN true 
                    ELSE false 
                END
            ''',
            'processType': 'PRE',
            'rank': 1
        },
        {
            'name': 'remove_duplicates',
            'query': '''
                DELETE FROM staging_data 
                WHERE id NOT IN (
                    SELECT MIN(id) 
                    FROM staging_data 
                    GROUP BY email, phone
                )
            ''',
            'processType': 'PRE',
            'rank': 2
        },
        {
            'name': 'enrich_data',
            'query': '''
                UPDATE staging_data 
                SET 
                    country = get_country_from_phone(phone),
                    age_group = CASE 
                        WHEN age < 25 THEN '18-24'
                        WHEN age < 35 THEN '25-34'
                        WHEN age < 45 THEN '35-44'
                        ELSE '45+'
                    END
            ''',
            'processType': 'POST',
            'rank': 1
        }
    ],
    'parameters': {
        'last_sync': '2024-01-01T00:00:00Z'
    },
    'queryEngine': 'SNOWFLAKE'
}
```

### Real-time Pipeline Monitoring

```javascript
// Real-time pipeline monitoring with WebSocket
class PipelineMonitor {
  constructor(token) {
    this.token = token;
    this.ws = null;
    this.callbacks = new Map();
  }

  connect() {
    this.ws = new WebSocket(`wss://api.example.com/pipeline-monitor?token=${this.token}`);
    
    this.ws.onmessage = (event) => {
      const data = JSON.parse(event.data);
      this.handleMessage(data);
    };

    this.ws.onclose = () => {
      console.log('Pipeline monitor disconnected');
      // Reconnect after 5 seconds
      setTimeout(() => this.connect(), 5000);
    };
  }

  handleMessage(data) {
    switch (data.type) {
      case 'JOB_STARTED':
        this.notifyCallbacks('jobStarted', data);
        break;
      case 'JOB_COMPLETED':
        this.notifyCallbacks('jobCompleted', data);
        break;
      case 'JOB_FAILED':
        this.notifyCallbacks('jobFailed', data);
        break;
      case 'PIPELINE_STATUS_CHANGED':
        this.notifyCallbacks('pipelineStatusChanged', data);
        break;
    }
  }

  on(event, callback) {
    if (!this.callbacks.has(event)) {
      this.callbacks.set(event, []);
    }
    this.callbacks.get(event).push(callback);
  }

  notifyCallbacks(event, data) {
    const callbacks = this.callbacks.get(event) || [];
    callbacks.forEach(callback => callback(data));
  }

  disconnect() {
    if (this.ws) {
      this.ws.close();
    }
  }
}

// Usage
const monitor = new PipelineMonitor('your-jwt-token');

monitor.on('jobStarted', (data) => {
  console.log(`Job ${data.jobId} started for pipeline ${data.pipelineId}`);
});

monitor.on('jobCompleted', (data) => {
  console.log(`Job ${data.jobId} completed successfully`);
});

monitor.on('jobFailed', (data) => {
  console.error(`Job ${data.jobId} failed: ${data.error}`);
  // Send notification or alert
});

monitor.connect();
```
